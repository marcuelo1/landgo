import 'package:landgo_seller/core/models/size_model.dart';

class ProductSize {
  SizeModel size;
  double price;
  double basePrice;

  ProductSize({
    required this.size,
    required this.price,
    required this.basePrice
  });
}